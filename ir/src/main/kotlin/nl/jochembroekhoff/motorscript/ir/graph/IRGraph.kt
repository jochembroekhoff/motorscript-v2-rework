package nl.jochembroekhoff.motorscript.ir.graph

class IRGraph {
    /*
    private val vertices = DoublyLinkedPositionalList<IRVertex>()
    private val edges = DoublyLinkedPositionalList<IREdge<*>>()

    val sizeV
        get() = vertices.size
    val sizeE
        get() = edges.size

    /* Read operations */

    fun <T : IRVertex> edgeBetween(first: T, second: T): IREdge<T>? {
        return null
    }

    fun <T : IRVertex> outgoing(v: T): Sequence<IREdge<T>> {

    }
    */
}
